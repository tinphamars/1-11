version: "3.9"

services:
  rag-chatbot-api:
    build: .
    container_name: rag-chatbot-api
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped
